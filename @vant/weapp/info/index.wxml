<wxs src="../wxs/utils.wxs" module="utils" />

<view
  wx:if="{{ info !== null && info !== '' && info>0 || dot }}"
  class="van-info {{ utils.bem('info', { dot }) }} custom-class"
  style="{{ customStyle }}"
>{{ dot ? '' : info }}</view>
